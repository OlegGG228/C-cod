import random

# Генерируем случайное число (0 или 1)
result = random.randint(0, 1)

# Проверяем результат и выводим соответствующее сообщение на экран
if result == 0:
    print("Решка")
else:
    print("Орел")
