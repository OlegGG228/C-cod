km_h = float(input("Введите скорость в километрах в час: "))
m_s = float(input("Введите скорость в метрах в секунду: "))

km_h_to_m_s = km_h * 1000 / 3600

if km_h_to_m_s > m_s:
    print("Скорость в километрах в час больше")
else:
    print("Скорость в метрах в секунду больше")