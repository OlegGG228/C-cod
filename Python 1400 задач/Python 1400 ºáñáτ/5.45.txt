a, b = 1, 1 # первые два элемента последовательности
for i in range(2, 10): # находим третий, четвертый и следующие элементы
    fib = a + b
    a, b = b, fib # обновляем значения prev1 и prev2
    # выводим результат
    if i in range(3, 11):
        print(f"Член на позиции {i}: {fib}")
