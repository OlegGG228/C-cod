arr = [1, 2, 3, 4, 5]

# Сохраняем первый элемент в переменную
first = arr[0]

# Сдвигаем элементы влево на 1 позицию
for i in range(1, len(arr)):
    arr[i-1] = arr[i]

# Перемещаем первый элемент на место последнего
arr[-1] = first

print(arr) # [2, 3, 4, 5, 1]

