array = [i for i in range(1, 21)]  # создаем массив из 20 элементов

array[:3], array[-3:] = array[-3:], array[:3]  # меняем первые три и последние три элемента местами

print(array)  # выводим полученный массив на экран

